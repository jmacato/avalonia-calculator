//  // #pragma  once

namespace CalculatorApp.ViewModel
{
    namespace Common
    {
        public enum BitLength : int
        {
            None = 0,
            Unknown = -1,
            Byte = 8,
            Word = 16,
            DoubleWord = 32,
            QuadWord = 64,
        };
    }
}
