//  // #pragma  once

namespace CalculatorApp.ViewModel
{
    namespace Common
    {
        public enum BitLength
        {
            Unknown = -1,
            None = 0,
            EightBits = 8,
            SixteenBits = 16,
            ThirtyTwoBits = 32,
            SixtyFourBits = 64,
        }
    }
}
